'use babel';

import LanguageLiterateJlView from '../lib/language-literate-jl-view';

describe('LanguageLiterateJlView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
